    <%- include('partials/head.ejs') %>
    <title>Feed | Marcel The Outdoor Cat</title>
</head>
<body>
    <header>
        <%- include('partials/header.ejs') %>        
    </header>

    <main class="">
      <div class="">
        <ul class="">
          <% for(var i=0; i<posts.length; i++) {%>
            <li class="">
              <!-- <a href="/post/<%= posts[i]._id%>"> -->
                <p><%= posts[i].catName%></p>
                <img class="" src="<%= posts[i].imageUrl%>">
                <p>
                  Likes: <%= posts[i].greatCat.length %>
                  <img class="" src="assets/icons/like-<%= posts[i].hidden %>.svg">
                </p>
                <p><%= posts[i].caption%></p>
              <!-- </a> -->
            </li>
          <% } %>
          </ul>
      </div>
      
      <p>It will eventually look like this:</p>
      <img src="assets/demo-Feed.jpg" alt="">
    </main>

    <footer>
        <%- include('partials/footer.ejs') %>